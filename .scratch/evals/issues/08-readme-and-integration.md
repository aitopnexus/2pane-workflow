# 08: evals README and integration sweep

**What to build:** The eval is documented and trustworthy as a whole. `evals/README.md` explains the three commands and their required model arguments, the one-time trust bootstrap for the fixed eval workdir, all optional parameters (runs, timeouts, turn cap, saving threshold, baseline pinning), the baseline lifecycle (creation, reuse without model calls, explicit refresh, why it never expires silently), and how to read statuses — pass/protocol-fail/infra-fail/economy-fail/baseline-missing, the separation of infra failures from model quality, and why single-run economy results are exploratory rather than statistical. The run-artifact and baseline directories are gitignored such that clearing results never touches baselines. Finally, the full validation sweep from the spec passes: syntax check, existing repository tests, all grader/cache/formula self-tests, and at least one live smoke pass of protocol S1, the baseline, and two economy runs sharing one baseline ID with the second run creating no new Expert-only session.

**Blocked by:** 03: Full protocol suite S1–S4 with per-scenario aggregation; 04: Infra-fail taxonomy with model pinning and negative self-tests; 07: Economy comparison, saving gate and cost reporting

**Status:** resolved

- [x] `evals/README.md` covers commands, trust bootstrap, parameters, baseline lifecycle and status interpretation
- [x] Results and baselines directories are gitignored; removing results leaves baselines intact
- [x] Validation sweep green: syntax check, repo tests, all self-test suites (grader fixtures, negative detection, cache behavior, economy formula)
- [x] Live smoke: protocol S1 run produces full metadata; baseline command works; two economy runs with different Main models reference the same baseline ID, and the second does not create a new Expert-only session

## Comments

`evals/README.md` documents the three commands and required model arguments, the fixed eval workdir and why eval runs need no interactive trust approval (skills pinned via `--no-skills --skill`, with the one-time prompt only if you open pi interactively in the workdir), every optional parameter, the baseline lifecycle (creation, reuse without model calls, explicit refresh, never expiring silently, fingerprint components and their deliberate exclusions), all five statuses plus lock-busy with the infra/quality separation, `expert-skipped`, and why single-run economy results are exploratory. `.gitignore` already covered `evals/results/` and `evals/baselines/`; the self-test asserts deleting results leaves the store intact. Validation sweep 2026-08-22: `bash -n` clean; `tests/test-*.sh` all green (15/15/3/25); `evals/run.sh self-test` 215 checks, 0 failures (grader fixtures, negative detections, violation matrix, baseline cache, economy formula + driver stubs). Live smoke: protocol suite 4/4 pass on openai-codex/gpt-5.6-luna:high with full metadata; `baseline` reused bl-20260822T112845Z-b6c55dba without a model call; economy run 1 (luna:high) classified protocol-fail — a genuine model violation of the eval's bash-only-for-helper rule (`ls` exploration), correctly separated from the economy verdict; economy run 2 (luna) passed expert-skipped at 100% saving; both referenced the same baseline ID and the store stayed at one baseline (no new Expert-only session). Real pi `--session` resume verified live: one JSONL, appended lines, context preserved.
